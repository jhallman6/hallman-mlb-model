import { chromium } from "playwright-core";
import fs from "node:fs/promises";
import path from "node:path";
const DATA=path.resolve("data"),PROFILE=path.resolve(".refresh-state/fangraphs-profile"),EDGE="C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe",SIZE=200;
const files=["fangraphs-batted-ball-2026.json","fangraphs-bat-tracking-2026.json","fangraphs-pitcher-batted-ball-2026.json","fangraphs-pitcher-standard-2026.json","fangraphs-pitcher-statcast-2026.json","fangraphs-pitcher-location-plus-2026.json"];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const num=v=>v===""||v==null?null:Number(String(v).replace(/[%,$]/g,""));
const norm=v=>String(v??"").toLowerCase().replace(/[^a-z0-9]+/g,"");
async function atomic(name,value){const t=path.join(DATA,name),n=`${t}.${process.pid}.new`;await fs.writeFile(n,JSON.stringify(value,null,2)+"\n");await fs.rename(n,t);}
async function grid(page){return page.locator(".fg-data-grid table").first().evaluate(table=>({headers:[...table.querySelectorAll("thead th")].map(x=>x.textContent.trim()),rows:[...table.querySelectorAll("tbody tr")].map(tr=>{const values=[...tr.querySelectorAll("td")].map(x=>x.textContent.trim());const href=tr.querySelector('a[href*="/players/"]')?.getAttribute('href')??'';return{values,playerId:Number(href.match(/\/players\/[^/]+\/(\d+)/)?.[1])||null}}),text:table.closest('.fg-data-grid')?.innerText??''}));}
async function collect(page,url){await page.goto(url,{waitUntil:"domcontentloaded",timeout:60000});await page.locator('.fg-data-grid table tbody tr').first().waitFor({state:'visible',timeout:60000});const selects=page.locator('.fg-data-grid select');await selects.first().selectOption(String(SIZE));await sleep(2200);let first=await grid(page);const expected=Number(first.text.match(/\d[\d,]*\s*-\s*\d[\d,]*\s+of\s+([\d,]+)\s+results/i)?.[1]?.replace(/,/g,''));if(!expected)throw new Error(`Result total missing for ${url}`);const all=[];for(let n=1;n<=Math.ceil(expected/SIZE);n++){let data;if(n===1)data=first;else{const input=page.locator('.fg-data-grid input.table-control-page').first();await input.fill(String(n));await input.press('Enter');await page.waitForFunction(x=>Number(document.querySelector('.fg-data-grid input.table-control-page')?.value)===x,n,{timeout:45000});await sleep(1300);data=await grid(page);}all.push(...data.rows);}if(all.length!==expected)throw new Error(`${url}: ${all.length}/${expected}`);return{headers:first.headers,rows:all,expected};}
function value(row,headers,...names){const keys=headers.map(norm);for(const name of names){const i=keys.indexOf(norm(name));if(i>=0)return row.values[i];}return null;}
const context=await chromium.launchPersistentContext(PROFILE,{executablePath:EDGE,headless:false});try{const page=context.pages()[0]??await context.newPage();for(const name of files){const old=JSON.parse(await fs.readFile(path.join(DATA,name),'utf8'));const url=old.sourceUrl??old.source;const got=await collect(page,url);let next;if(name==='fangraphs-batted-ball-2026.json'){next={...old,fetchedAt:new Date().toISOString(),rows:got.rows.map((r,i)=>({rank:i+1,playerId:r.playerId,name:value(r,got.headers,'Name'),team:value(r,got.headers,'Team'),gbPct:num(value(r,got.headers,'GB%'))/100,fbPct:num(value(r,got.headers,'FB%'))/100,hardHitPct:num(value(r,got.headers,'Hard%','HardHit%'))/100,pullPct:num(value(r,got.headers,'Pull%'))/100}))};}else if(name==='fangraphs-bat-tracking-2026.json'){next={...old,fetchedAt:new Date().toISOString(),rows:got.rows.map((r,i)=>({rank:i+1,playerId:r.playerId,name:value(r,got.headers,'Name'),team:value(r,got.headers,'Team'),competitiveSwings:num(value(r,got.headers,'Competitive Swings','Comp Swings')),batSpeed:num(value(r,got.headers,'Bat Speed')),fastSwingPct:num(value(r,got.headers,'Fast Swing%'))/100,swingLength:num(value(r,got.headers,'Swing Length')),squaredContactPct:num(value(r,got.headers,'Squared-Up Contact%'))/100,squaredSwingPct:num(value(r,got.headers,'Squared-Up Swing%'))/100,blastContactPct:num(value(r,got.headers,'Blast Contact%'))/100,blastSwingPct:num(value(r,got.headers,'Blast Swing%'))/100,tilt:num(value(r,got.headers,'Tilt')),attackAngle:num(value(r,got.headers,'Attack Angle')),attackDirection:num(value(r,got.headers,'Attack Direction')),idealAttackAnglePct:num(value(r,got.headers,'Ideal Attack Angle%'))/100}))};}else{next={...old,fetchedAt:new Date().toISOString(),headers:got.headers,rows:got.rows.map(r=>r.values),playerIds:got.rows.map(r=>r.playerId)};}await atomic(name,next);console.log(`Validated ${name}: ${got.expected}`);}}finally{await context.close();}
